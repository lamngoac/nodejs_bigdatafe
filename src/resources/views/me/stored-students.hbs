<form class="mt-4" name="container-form" method="POST" action="/students/handle-form-actions">
    <div>
      <h3>Học viên của tôi</h3>
      <a href="/me/trash/students">Học viên đã xóa ({{this.deletedCount}})</a>

      <div class="mt-4 d-flex align-items-center">
        <div class="form-check">
          <input class="form-check-input" type="checkbox" value="" id="checkbox-all">
          <label class="form-check-label" for="checkbox-all">
            Chọn tất cả
          </label>
        </div>

        <select class="form-control form-control-sm checkbox-sellect-all" name="action" required>
          <option value="">-- Chọn hành động --</option>
          <option value="delete">Xóa</option>
        </select>

        <button class="btn btn-primary btn-sm btn-submit-all-checkbox" type="submit" disabled>Thực hiện</button>
      </div>
    </div>
    <table class="table mt-4">
        <thead>
            <tr>
            <th scope="col">#</th>
            <th scope="col">STT</th>
            <th scope="col">Tên học viên</th>
            <th scope="col">Mã học viên</th>
            <th scope="col">Khóa</th>
            <th scope="col">Chức năng</th>
            </tr>
        </thead>
        <tbody>
            {{#each students}}
            <tr>
            <td>
              <div class="form-check">
                <input class="form-check-input" type="checkbox" name="studentIds[]" value="{{this._id}}">
              </div>
            </td>
            <td scope="row">{{sum @index 1}}</td>
            <td>{{this.fullName}}</td>
            <td>{{this.studentCode}}</td>
            <td>{{this.gene}}</td>
            <td>
                <a href="/students/{{this._id}}/edit" class="btn btn-link">Sửa</a>
                <a href="" class="btn btn-link" data-toggle="modal" data-id="{{this._id}}" data-target="#delete-course-modal">Xóa</a>
            </td>
            </tr>
            {{else}}
            <tr>
              <td colspan="5" class="text-center">
                Chưa có học viên nào.
                <a href="/students/create">Thêm học viên</a>
              </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</form>

{{!--Confirm for deleting course--}}
<div id="delete-course-modal" class="modal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Xóa học viên ?</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>Bạn chắc chắn muốn xóa học viên này ?</p>
      </div>
      <div class="modal-footer">
        <button id="btn-delete-course" type="button" class="btn btn-danger">Xóa bỏ</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Hủy bỏ</button>
      </div>
    </div>
  </div>
</div>

{{!--Form delete course--}}
<form class="mt-4" method="POST" name="delete-course-form"></form>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        var studentId;
        var deleteCourseForm = document.forms['delete-course-form'];
        var containerForm = $('form[name="container-form"]');
        var btnDeleteCourse = document.getElementById('btn-delete-course');
        var checkboxAll = $('#checkbox-all');
        var studentItemCheckbox = $('input[name="studentIds[]"]');
        var checkAllSubmitBtn = $('.btn-submit-all-checkbox');

        $('#delete-course-modal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget);
            studentId = button.data('id');
        })

        btnDeleteCourse.onclick = function() {
            deleteCourseForm.action = '/students/' + studentId + '?_method=DELETE';
            deleteCourseForm.submit();
        }

        checkboxAll.change(function() {
          var isCheckAll = $(this).prop('checked');
          studentItemCheckbox.prop('checked', isCheckAll);
          renderCheckAllSubmitBtn();
        })

        studentItemCheckbox.change(function() {
          var isCheckAll = studentItemCheckbox.length === studentItemCheckbox.filter(':checked').length;
          checkboxAll.prop('checked', isCheckAll);
          renderCheckAllSubmitBtn();
        })

        // Re-render checkall submit button
        function renderCheckAllSubmitBtn() {
          var checkedCount = studentItemCheckbox.filter(':checked').length;
          if (checkedCount > 0) {
            checkAllSubmitBtn.attr('disabled', false);
          } else {
            checkAllSubmitBtn.attr('disabled', true);
          }
        };
    });
</script>